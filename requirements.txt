# Gate-minimal. Roboflow SDK / OpenCV / NumPy / Shapely land with PRD §9 —
# keeping them out now is the difference between a fast first deploy and
# missing the 17:45 eligibility gate.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27
